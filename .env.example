# Seal rotor debug mode — set to 1 for 6s/9s rotation + visible notch/dot
# When 1: rotation obvious within 1 second; notch on outer ring, dot on inner
# When unset/0: production 60s/90s, subtle notch only
NEXT_PUBLIC_SEAL_DEBUG=0
